version: '3.3'

services:
  backend-app:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - backend-network
    restart: always

networks:
  backend-network:
    driver: bridge